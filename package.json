{
	"private": true,
	"scripts": {
		"test": "vitest run",
		"test:watch": "vitest",
		"lint": "eslint --fix --cache packages/"
	},
	"devDependencies": {
		"eslint": "^8.22.0",
		"vite": "^3.0.8",
		"vitest": "^0.20.3"
	}
}
