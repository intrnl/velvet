{
	"private": true,
	"scripts": {
		"test": "vitest run",
		"test:watch": "vitest",
		"lint": "eslint --fix --cache packages/"
	},
	"devDependencies": {
		"eslint": "^8.8.0",
		"vitest": "^0.4.2"
	}
}
