{
	"private": true,
	"scripts": {
		"test": "vitest run",
		"test:watch": "vitest",
		"lint": "eslint --fix --cache packages/"
	},
	"devDependencies": {
		"eslint": "^8.14.0",
		"vitest": "^0.10.2"
	}
}
