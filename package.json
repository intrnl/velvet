{
	"private": true,
	"scripts": {
		"test": "vitest run",
		"test:watch": "vitest",
		"lint": "eslint --fix --cache packages/"
	},
	"devDependencies": {
		"eslint": "^8.23.0",
		"vite": "^3.1.0",
		"vitest": "^0.23.2"
	}
}
