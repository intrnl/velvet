{
	"private": true,
	"scripts": {
		"test": "vitest run",
		"test:watch": "vitest",
		"lint": "eslint --fix --cache packages/"
	},
	"devDependencies": {
		"eslint": "^8.16.0",
		"vite": "^2.9.9",
		"vitest": "^0.12.9"
	}
}
